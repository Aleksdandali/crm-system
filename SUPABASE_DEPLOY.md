# Деплой CRM на Supabase + Vercel

## 📋 Предварительная подготовка

### 1. Создайте аккаунты:
- [Supabase](https://supabase.com) - для базы данных
- [Vercel](https://vercel.com) - для хостинга фронтенда
- [Railway](https://railway.app) или [Render](https://render.com) - для бэкенда API

## 🗄️ Шаг 1: Настройка Supabase

### 1.1 Создайте проект в Supabase
1. Перейдите на https://app.supabase.com
2. Нажмите "New Project"
3. Заполните данные:
   - **Name**: CRM System
   - **Database Password**: (создайте надёжный пароль)
   - **Region**: выберите ближайший к вам регион

### 1.2 Получите данные подключения
После создания проекта:
1. Перейдите в **Settings** → **Database**
2. Скопируйте **Connection String** (URI)
3. Она будет выглядеть так:
```
postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
```

### 1.3 Настройте таблицы
Выполните SQL миграции из файлов в `backend/src/migrations/`:

1. Откройте **SQL Editor** в Supabase
2. Скопируйте содержимое каждого файла миграции по порядку:
   - `20240101000001-create-users.cjs`
   - `20240101000002-create-companies.cjs`
   - И так далее...
3. Выполните каждый SQL скрипт

**Или** используйте Supabase CLI:
```bash
# Установите Supabase CLI
npm install -g supabase

# Войдите в аккаунт
supabase login

# Свяжите с проектом
supabase link --project-ref [YOUR-PROJECT-REF]

# Примените миграции (нужно адаптировать миграции)
```

## 🚀 Шаг 2: Деплой Backend на Railway

### 2.1 Подготовка
1. Создайте аккаунт на https://railway.app
2. Установите Railway CLI:
```bash
npm install -g @railway/cli
```

### 2.2 Деплой
```bash
cd backend

# Войдите в Railway
railway login

# Инициализируйте проект
railway init

# Добавьте переменные окружения
railway variables set NODE_ENV=production
railway variables set DB_HOST=[SUPABASE-HOST]
railway variables set DB_PORT=5432
railway variables set DB_NAME=postgres
railway variables set DB_USER=postgres
railway variables set DB_PASSWORD=[YOUR-SUPABASE-PASSWORD]
railway variables set JWT_SECRET=[GENERATE-STRONG-SECRET]
railway variables set JWT_REFRESH_SECRET=[GENERATE-ANOTHER-SECRET]

# Задеплойте
railway up
```

**Или используйте Render.com:**
1. Создайте аккаунт на https://render.com
2. Нажмите "New +" → "Web Service"
3. Подключите GitHub репозиторий
4. Настройки:
   - **Build Command**: `cd backend && npm install`
   - **Start Command**: `cd backend && npm start`
5. Добавьте Environment Variables (как выше)

### 2.3 Получите URL бэкенда
После деплоя вы получите URL типа:
- Railway: `https://your-app.railway.app`
- Render: `https://your-app.onrender.com`

Сохраните этот URL - он понадобится для фронтенда!

## 🎨 Шаг 3: Деплой Frontend на Vercel

### 3.1 Подготовка репозитория
```bash
cd ~/crm-system

# Инициализируйте Git (если ещё не сделали)
git init
git add .
git commit -m "Initial commit: CRM System"

# Создайте репозиторий на GitHub и запушьте
git remote add origin [YOUR-GITHUB-REPO]
git push -u origin main
```

### 3.2 Деплой на Vercel
1. Перейдите на https://vercel.com
2. Нажмите "Add New" → "Project"
3. Импортируйте ваш GitHub репозиторий
4. Настройте проект:
   - **Framework Preset**: Vite
   - **Root Directory**: `frontend`
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`

### 3.3 Добавьте переменные окружения
В настройках Vercel добавьте:
```
VITE_API_URL=https://your-backend.railway.app/api
```

### 3.4 Задеплойте
Нажмите "Deploy" и подождите несколько минут.

## ✅ Шаг 4: Проверка

После деплоя:

1. **Frontend**: `https://your-project.vercel.app`
2. **Backend**: `https://your-backend.railway.app/health`
3. **Database**: подключена через Supabase

### Проверьте работу:
1. Откройте фронтенд URL
2. Зарегистрируйте нового пользователя
3. Войдите в систему
4. Проверьте работу модулей

## 🔧 Альтернатива: Redis

Поскольку мы используем Supabase, Redis нужно заменить на альтернативу:

### Вариант 1: Upstash Redis (рекомендуется)
1. Создайте аккаунт на https://upstash.com
2. Создайте Redis базу (бесплатный план)
3. Получите URL подключения
4. Добавьте в переменные окружения:
```
REDIS_HOST=[UPSTASH-HOST]
REDIS_PORT=[UPSTASH-PORT]
REDIS_PASSWORD=[UPSTASH-PASSWORD]
```

### Вариант 2: Убрать Redis
Если Redis не критичен, можно временно отключить:
- Закомментировать код с `redisClient` в `backend/src/server.js`
- Использовать только JWT без blacklist

## 📊 Мониторинг

- **Supabase Dashboard**: мониторинг базы данных
- **Vercel Dashboard**: логи фронтенда
- **Railway/Render Dashboard**: логи бэкенда

## 💰 Стоимость (бесплатные лимиты)

- **Supabase Free**: 500MB база, 2GB передачи
- **Vercel Free**: 100GB bandwidth, неограниченные деплои
- **Railway Free**: $5 кредитов в месяц
- **Render Free**: 750 часов в месяц

## 🔐 Безопасность

После деплоя обязательно:
1. ✅ Смените все пароли и секреты
2. ✅ Настройте CORS только для вашего домена
3. ✅ Включите SSL/HTTPS
4. ✅ Настройте backup базы данных в Supabase
5. ✅ Ограничьте доступ к API через API keys

---

Готовы начать? Начнём с Supabase! 🚀
