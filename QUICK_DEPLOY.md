# 🚀 Быстрый деплой CRM на Supabase + Vercel

## Шаг 1: Supabase (5 минут)

1. Перейдите на https://app.supabase.com
2. Нажмите **"New Project"**
3. Заполните:
   - Name: `CRM System`
   - Database Password: (придумайте надёжный пароль)
   - Region: `Europe West` (или ближайший к вам)
4. Нажмите **"Create new project"** и подождите 2-3 минуты

5. После создания:
   - Перейдите в **SQL Editor** (иконка слева)
   - Нажмите **"New query"**
   - Скопируйте весь код из файла `supabase-migrations.sql`
   - Вставьте и нажмите **"Run"** или `Ctrl+Enter`

6. Получите данные подключения:
   - Перейдите в **Settings** → **Database**
   - Скопируйте **URI** в разделе "Connection string"
   - Формат: `postgresql://postgres:[PASSWORD]@db.[REF].supabase.co:5432/postgres`

## Шаг 2: Подготовка кода (2 минуты)

```bash
cd ~/crm-system

# Инициализируйте Git
git init
git add .
git commit -m "Initial commit"

# Создайте репозиторий на GitHub
# Затем:
git remote add origin https://github.com/ваш-username/crm-system.git
git branch -M main
git push -u origin main
```

## Шаг 3: Деплой Backend на Render (5 минут)

1. Перейдите на https://render.com и войдите через GitHub
2. Нажмите **"New +"** → **"Web Service"**
3. Выберите ваш репозиторий `crm-system`
4. Настройки:
   - **Name**: `crm-backend`
   - **Root Directory**: `backend`
   - **Runtime**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Instance Type**: `Free`

5. Добавьте Environment Variables:
   ```
   NODE_ENV=production
   PORT=5000
   
   # Скопируйте из Supabase Connection String:
   DB_HOST=db.[ваш-ref].supabase.co
   DB_PORT=5432
   DB_NAME=postgres
   DB_USER=postgres
   DB_PASSWORD=[ваш-пароль-из-supabase]
   
   # Генерируйте случайные строки:
   JWT_SECRET=ваш-случайный-секрет-минимум-32-символа
   JWT_REFRESH_SECRET=другой-случайный-секрет-32-символа
   JWT_EXPIRE=15m
   JWT_REFRESH_EXPIRE=7d
   
   # Временно отключим Redis (добавим позже через Upstash)
   REDIS_HOST=
   REDIS_PORT=
   
   CORS_ORIGIN=*
   ```

6. Нажмите **"Create Web Service"**
7. Подождите 3-5 минут пока деплой завершится
8. Скопируйте URL вашего бэкенда (будет типа `https://crm-backend.onrender.com`)

## Шаг 4: Деплой Frontend на Vercel (3 минуты)

1. Перейдите на https://vercel.com и войдите через GitHub
2. Нажмите **"Add New..."** → **"Project"**
3. Выберите репозиторий `crm-system`
4. Настройки:
   - **Framework Preset**: `Vite`
   - **Root Directory**: `frontend`
   - **Build Command**: оставьте по умолчанию
   - **Output Directory**: оставьте по умолчанию

5. Environment Variables:
   ```
   VITE_API_URL=https://crm-backend.onrender.com/api
   ```
   (замените на ваш URL из Render)

6. Нажмите **"Deploy"**
7. Подождите 2-3 минуты

## Шаг 5: Проверка (1 минута)

1. Откройте ваш URL Vercel (будет типа `https://crm-system.vercel.app`)
2. Нажмите **"Register"**
3. Создайте аккаунт
4. Войдите в систему!

## ✅ Готово!

Ваша CRM система развёрнута и работает!

- 🌐 **Frontend**: https://ваш-проект.vercel.app
- 🔌 **Backend**: https://ваш-бэкенд.onrender.com
- 🗄️ **Database**: Supabase

## 🔧 Дополнительно: Добавить Redis (опционально)

Если нужен Redis для кэширования:

1. Создайте аккаунт на https://upstash.com
2. Создайте Redis базу
3. Получите credentials
4. Добавьте их в Environment Variables на Render:
   ```
   REDIS_HOST=ваш-upstash-host
   REDIS_PORT=ваш-upstash-port
   REDIS_PASSWORD=ваш-upstash-password
   ```

## 🐛 Проблемы?

**Backend не запускается:**
- Проверьте логи на Render
- Убедитесь что все ENV variables заполнены
- Проверьте Connection String от Supabase

**Frontend не подключается к backend:**
- Проверьте VITE_API_URL в настройках Vercel
- Убедитесь что backend работает (откройте /health endpoint)

**База данных не работает:**
- Проверьте что SQL миграции выполнены в Supabase
- Проверьте credentials в настройках Render

---

**Важно:** После первого деплоя Render может "засыпать" на бесплатном плане. Первый запрос займёт 30-60 секунд для "пробуждения".

Для production используйте платные планы! 💎
